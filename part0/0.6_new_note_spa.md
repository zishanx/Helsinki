```mermaid
sequenceDiagram
    participant user
    participant browser
    participant server

    user->>browser: Write a new note
    user->>browser: Click Save

    Note right of browser: JavaScript prevents the default form submission
    Note right of browser: JavaScript creates the note object

    browser->>server: POST /exampleapp/new_note_spa
    activate server
    Note right of server: Server saves the new note
    server-->>browser: HTTP 201 Created
    deactivate server

    Note right of browser: Browser updates the page with the new note